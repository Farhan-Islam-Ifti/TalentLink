<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Job Seeker Profile</title>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        /* Base Styles */
        body {
            font-family: 'Poppins', sans-serif;
            margin: 0;
            background: linear-gradient(135deg,#f3f4f6 0%,#e2e8f0 100%);
            color: #333;
        }

        .profile-container {
            min-height: 100vh;
            padding: 2rem 0;
        }

        .container {
            max-width: 1100px;
            margin: 0 auto;
            padding: 0 15px;
        }

        .row {
            display: flex;
            flex-wrap: wrap;
            margin: 0 -15px;
        }

        .justify-content-center {
            justify-content: center;
        }

        .col-md-8 {
            flex: 0 0 66.666667%;
            max-width: 66.666667%;
            padding: 0 15px;
        }

        /* Profile Card */
        .profile-card {
            background: rgba(255,255,255,0.95);
            border-radius: 20px;
            box-shadow: 0 20px 50px rgba(0,0,0,0.08);
            overflow: hidden;
            margin-bottom: 2rem;
            padding-bottom: 2rem;
            position: relative;
        }

        /* Header with animated background and website name */
        .profile-header-bg {
            height: 180px;
            position: relative;
            margin: -2rem -2rem 2rem -2rem;
            border-radius: 20px 20px 0 0;
            overflow: hidden;
            display: flex;
            justify-content: center;
            align-items: center;
        }

            .profile-header-bg::before {
                content: '';
                position: absolute;
                width: 150%;
                height: 150%;
                background: linear-gradient(120deg,#667eea,#764ba2,#89f7fe,#a1c4fd);
                top: -25%;
                left: -25%;
                animation: bgMove 20s linear infinite;
            }

        .header-title {
            position: relative;
            z-index: 2;
            font-size: 2rem;
            font-weight: 700;
            background: linear-gradient(90deg,#667eea,#764ba2,#89f7fe);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            animation: headerGlow 3s ease-in-out infinite alternate;
        }

        /* Profile Image */
        .profile-img {
            width: 160px;
            height: 160px;
            object-fit: cover;
            margin-top: -80px;
            border-radius: 50%;
            border: 5px solid #fff;
            box-shadow: 0 10px 25px rgba(0,0,0,0.15);
            transition: all 0.4s ease;
            position: relative;
            z-index: 2;
        }

            .profile-img:hover {
                transform: scale(1.08) rotate(-2deg);
            }

        /* Text Gradient */
        .text-gradient {
            background: linear-gradient(90deg,#667eea,#764ba2);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }

        /* Detail Cards */
        .detail-card {
            background: rgba(255,255,255,0.85);
            border-radius: 16px;
            padding: 2rem 1.5rem 1.5rem 1.5rem; /* more top padding to avoid icon cut */
            position: relative;
            transition: all 0.4s ease;
            overflow: hidden;
            height: 100%;
        }

            .detail-card::before {
                content: '';
                position: absolute;
                width: 200%;
                height: 200%;
                top: -50%;
                left: -50%;
                background: linear-gradient(120deg,#667eea,#89f7fe,#764ba2,#a1c4fd);
                background-size: 400% 400%;
                opacity: 0.1;
                animation: cardBgMove 15s ease infinite;
                z-index: 0;
            }

            .detail-card:hover {
                transform: translateY(-8px);
                box-shadow: 0 20px 40px rgba(0,0,0,0.12);
                background: rgba(255,255,255,0.95);
            }

            .detail-card h6, .detail-card p, .skill-tag, .btn-outline-primary {
                position: relative;
                z-index: 1;
            }

        @@keyframes cardBgMove {
            0% {
                background-position: 0% 50%;
            }

            50% {
                background-position: 100% 50%;
            }

            100% {
                background-position: 0% 50%;
            }
        }

        /* Detail Icons */
        .detail-icon {
            position: absolute;
            top: 0px; /* adjusted to avoid cutting */
            right: 15px;
            width: 45px;
            height: 45px;
            border-radius: 50%;
            background: linear-gradient(135deg,#667eea,#764ba2);
            display: flex;
            align-items: center;
            justify-content: center;
            color: #fff;
            font-size: 1.3rem;
            box-shadow: 0 6px 15px rgba(0,0,0,0.1);
            z-index: 2;
        }

        /* Skills */
        .skills-container {
            display: flex;
            flex-wrap: wrap;
            gap: 0.5rem;
            z-index: 1;
            position: relative;
        }

        .skill-tag {
            background: linear-gradient(135deg,#667eea,#764ba2);
            color: #fff;
            padding: 0.35rem 0.75rem;
            border-radius: 20px;
            font-size: 0.85rem;
            transition: 0.3s;
        }

            .skill-tag:hover {
                transform: scale(1.1);
            }

        /* Buttons */
        .btn-primary {
            background: linear-gradient(135deg,#667eea,#764ba2);
            color: #fff;
            border-radius: 50px;
            padding: 0.5rem 1.5rem;
            font-weight: 500;
            transition: all 0.3s ease;
        }

            .btn-primary:hover {
                transform: translateY(-3px) scale(1.05);
                box-shadow: 0 10px 25px rgba(75,108,183,0.4);
            }

        .btn-outline-primary {
            border: 2px solid #667eea;
            color: #667eea;
            border-radius: 50px;
            padding: 0.25rem 0.75rem;
            font-size: 0.85rem;
            transition: all 0.3s ease;
        }

            .btn-outline-primary:hover {
                background: #667eea;
                color: #fff;
                transform: scale(1.05);
            }

        /* Close Button */
        .btn-close-position {
            position: absolute;
            top: 20px;
            right: 20px;
            width: 40px;
            height: 40px;
            border-radius: 50%;
            background: #fff;
            display: flex;
            align-items: center;
            justify-content: center;
            color: #6c757d;
            text-decoration: none;
            box-shadow: 0 6px 15px rgba(0,0,0,0.1);
            transition: all 0.3s ease;
            z-index: 10;
        }

            .btn-close-position:hover {
                background: #f0f0f0;
                transform: rotate(90deg);
                color: #667eea;
            }

        /* Animations */
        .animated-fade-in {
            animation: fadeIn 0.8s ease-out forwards;
        }

        .animated-slide-in-left {
            opacity: 0;
            transform: translateX(-30px);
            animation: slideInLeft 0.6s ease-out forwards;
        }

        .animated-slide-in-right {
            opacity: 0;
            transform: translateX(30px);
            animation: slideInRight 0.6s ease-out forwards;
        }

        .delay-1 {
            animation-delay: 0.1s;
        }

        .delay-2 {
            animation-delay: 0.2s;
        }

        .delay-3 {
            animation-delay: 0.3s;
        }

        .delay-4 {
            animation-delay: 0.4s;
        }

        .delay-5 {
            animation-delay: 0.5s;
        }

        .delay-6 {
            animation-delay: 0.6s;
        }

        @@keyframes fadeIn {
            from {
                opacity: 0;
            }

            to {
                opacity: 1;
            }
        }

        @@keyframes slideInLeft {
            to {
                opacity: 1;
                transform: translateX(0);
            }
        }

        @@keyframes slideInRight {
            to {
                opacity: 1;
                transform: translateX(0);
            }
        }

        @@keyframes bgMove {
            0% {
                transform: rotate(0deg);
            }

            50% {
                transform: rotate(180deg);
            }

            100% {
                transform: rotate(360deg);
            }
        }

        @@keyframes headerGlow {
            0% {
                filter: drop-shadow(0 0 5px #667eea);
            }

            50% {
                filter: drop-shadow(0 0 15px #764ba2);
            }

            100% {
                filter: drop-shadow(0 0 5px #89f7fe);
            }
        }

        /* Responsive */
        @@media(max - width:768px) {
            .col-md-8

        {
            flex: 0 0 100%;
            max-width: 100%;
        }

        .profile-header-bg {
            height: 140px;
        }

        .profile-img {
            width: 120px;
            height: 120px;
            margin-top: -60px;
        }

        .flex-md-row {
            flex-direction: column !important;
            text-align: center;
        }

        .text-md-start {
            text-align: center !important;
        }

        .justify-content-md-start {
            justify-content: center !important;
        }

        }
    </style>
</head>
<body>
    <div class="profile-container">
        <div class="container my-5 animated-fade-in">
            <div class="row justify-content-center">
                <div class="col-md-8">
                    <div class="card profile-card shadow-lg border-0 rounded-4">
                        <div class="profile-header-bg">
                            <div class="header-title">TalentLink</div>
                        </div>
                        <div class="position-relative pt-5">
                            <a href="javascript:history.back()" class="btn-close-position"><i class="fas fa-times"></i></a>
                            <div class="px-4">
                                <!-- Profile Header -->
                                <div class="d-flex flex-column flex-md-row align-items-center text-center text-md-start mb-4">
                                    <div class="position-relative me-md-4 mb-3 mb-md-0">
                                        <img src="@(!string.IsNullOrEmpty(Model.Image) ? Model.Image : Url.Content("~/images/profile.jpg"))"
                                             alt="Profile Image" class="profile-img">
                                    </div>
                                    <div class="flex-grow-1">
                                        <h2 class="mb-1 fw-bold text-gradient">@Model.FirstName @Model.LastName</h2>
                                        <div class="d-flex flex-column flex-md-row align-items-center justify-content-center justify-content-md-start">
                                            <p class="text-muted mb-1 me-md-3"><i class="fas fa-envelope me-2"></i>@Model.Email</p>
                                            <p class="text-muted mb-1"><i class="fas fa-phone me-2"></i>@Model.PhoneNumber</p>
                                        </div>
                                        <div class="mt-3">
                                            <a asp-action="EditProfile" asp-route-userId="@Model.Id" class="btn btn-primary"><i class="fas fa-edit me-2"></i> Edit Profile</a>
                                        </div>
                                    </div>
                                </div>

                                <hr class="my-4" />

                                <!-- Profile Details Grid -->
                                <div class="row text-start">
                                    <div class="col-md-6 mb-3 animated-slide-in-left delay-1">
                                        <div class="detail-card h-100">
                                            <div class="detail-icon"><i class="fas fa-tools"></i></div>
                                            <h6 class="text-secondary">Skills</h6>
                                            <div class="skills-container">
                                                @if (!string.IsNullOrEmpty(Model.Skills))
                                                {
                                                    var skills = Model.Skills.Split(',');
                                                    foreach (var skill in skills)
                                                    {
                                                        <span class="skill-tag">@skill.Trim()</span>
                                                    }
                                                }
                                                else
                                                {

                                                    <p class="text-muted">-</p>
                                                }
                                            </div>
                                        </div>
                                    </div>

                                    <div class="col-md-6 mb-3 animated-slide-in-right delay-2">
                                        <div class="detail-card h-100">
                                            <div class="detail-icon"><i class="fas fa-briefcase"></i></div>
                                            <h6 class="text-secondary">Experience</h6>
                                            <p>@(Model.Experience ?? "-")</p>
                                        </div>
                                    </div>

                                    <div class="col-md-6 mb-3 animated-slide-in-left delay-3">
                                        <div class="detail-card h-100">
                                            <div class="detail-icon"><i class="fas fa-graduation-cap"></i></div>
                                            <h6 class="text-secondary">Education</h6>
                                            <p>@(Model.Education ?? "-")</p>
                                        </div>
                                    </div>

                                    <div class="col-md-6 mb-3 animated-slide-in-right delay-4">
                                        <div class="detail-card h-100">
                                            <div class="detail-icon"><i class="fas fa-map-marker-alt"></i></div>
                                            <h6 class="text-secondary">Address</h6>
                                            <p>@(Model.Address ?? "-")</p>
                                        </div>
                                    </div>

                                    <div class="col-md-6 mb-3 animated-slide-in-left delay-5">
                                        <div class="detail-card h-100">
                                            <div class="detail-icon"><i class="fas fa-birthday-cake"></i></div>
                                            <h6 class="text-secondary">Date of Birth</h6>
                                            <p>@(Model.DateOfBirth != null ? ((DateTime)Model.DateOfBirth).ToString("dd MMM yyyy") : "-")</p>
                                        </div>
                                    </div>

                                    <div class="col-md-6 mb-3 animated-slide-in-right delay-6">
                                        <div class="detail-card h-100">
                                            <div class="detail-icon"><i class="fas fa-file-pdf"></i></div>
                                            <h6 class="text-secondary">Resume</h6>
                                            @if (!string.IsNullOrEmpty(Model.CVFilePath))
                                            {
                                                <a href="@Model.CVFilePath" target="_blank" class="btn btn-outline-primary btn-sm">View PDF</a>
                                            }
                                            else
                                            {

                                                <span class="text-muted">Not uploaded</span>
                                            }
                                        </div>
                                    </div>
                                </div>

                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <script>
            document.addEventListener('DOMContentLoaded', function() {
                const cards = document.querySelectorAll('.detail-card');
                cards.forEach(card => {
                    card.addEventListener('mouseenter', () => card.style.transform='translateY(-8px)');
                    card.addEventListener('mouseleave', () => card.style.transform='translateY(0)');
                });
            });
        </script>
</body>
</html>
